// NOTE: As of Tailwind v3, we are using the standard 'tailwindcss' and 'autoprefixer' plugins.
// The previous '@tailwindcss/postcss' plugin is no longer required.
// Please see the README for migration details and breaking changes.

const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
